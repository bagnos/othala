<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="it.othala.images.queries">


	<select id="listSiteImages" resultType="it.othala.dto.SiteImagesDTO">


		SELECT
		idsiteImages,
		txGroupImages,
		pgGroupImages,
		txLibrary,
		txName,
		urlRedirect
		FROM
		${database.schema}site_images
		where txGroupImages != 'newArrivals'

		union all

		SELECT
		idsiteImages,
		txGroupImages,
		pgGroupImages,
		txLibrary,
		txName,
		urlRedirect
		FROM
		${database.schema}site_images a
		where txGroupImages = 'newArrivals';
		



	</select>
	
	<delete id="deleteSiteImages" parameterType="java.lang.String">

		DELETE FROM 
		${database.schema}site_images
		WHERE
		txGroupImages = #{groupImage}

	</delete>
	
	<insert id="insertSiteImages" parameterType="it.othala.dto.SiteImagesDTO">

		INSERT INTO
		${database.schema}site_images
		(`txGroupImages`,`pgGroupImages`,`txLibrary`,`txName`,`urlRedirect`)
		values
		(#{txGroupImages},#{pgGroupImages},#{txLibrary},#{txName},#{urlRedirect})

	</insert>


	<select id="getImage" parameterType="java.lang.String"  resultType="it.othala.dto.SiteImagesDTO">

		SELECT
		idsiteImages,
		txGroupImages,
		pgGroupImages,
		txLibrary,
		txName,
		urlRedirect
		FROM
		${database.schema}site_images
		where txGroupImages = #{string}

	</select>


</mapper>